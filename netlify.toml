[build]
  command = "pnpm install --frozen-lockfile && cd apps/web && pnpm run build"
  publish = "apps/web/out"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
